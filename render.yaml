services:
  - type: web
    name: smart-queue-api
    runtime: node
    region: singapore
    plan: free
    rootDir: .
    buildCommand: npx pnpm@9.15.9 install --frozen-lockfile=false && npx pnpm@9.15.9 --filter @workspace/api-server run build
    startCommand: node --enable-source-maps ./artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        value: file:./local.db
